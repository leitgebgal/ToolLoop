apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: item-mongo
  name: item-mongo
spec:
  ports:
    - name: "27017"
      port: 27017
      targetPort: 27017
  selector:
    io.kompose.service: item-mongo
