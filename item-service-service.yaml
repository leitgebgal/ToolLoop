apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: item-service
  name: item-service
spec:
  ports:
    - name: "8080"
      port: 8080
      targetPort: 8080
    - name: "50051"
      port: 50051
      targetPort: 50051
  selector:
    io.kompose.service: item-service
