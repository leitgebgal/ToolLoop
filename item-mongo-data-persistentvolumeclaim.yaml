apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: item-mongo-data
  name: item-mongo-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
